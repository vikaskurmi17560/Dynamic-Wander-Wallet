import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;


module.exports = {
  images: {
    domains: ['assets.aceternity.com','uttarakhandtourism.net'], // Add the domain here
  },
};